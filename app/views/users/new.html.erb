<%= form_for @user, url: users_path, html: { method: :post } do |f| %>
<h1>ユーザー作成</h1>
<form>
  <p>氏名：<%= f.text_field :name %></p>
  <p>年齢：<%= f.text_field :age %></p>
  <input type="submit" value="送信">
</form>
<a href="/users">一覧画面へ</a>
<% end %>
